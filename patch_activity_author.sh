sed -i 's/TextView mangaTitleDetail = findViewById(R.id.mangaTitleDetail);/TextView mangaTitleDetail = findViewById(R.id.mangaTitleDetail);\n        TextView authorText = findViewById(R.id.mangaAuthor);/g' app/src/main/java/com/fire/mangareader/activity/MangaDetailActivity.java
sed -i '/String tempStatus = "مستمرة";/a \
                Element authorElement = doc.select(".author-content, .author-info, .manga-author, .post-content_item:contains(Author) .summary-content, .post-content_item:contains(المؤلف) .summary-content").first();\n                String tempAuthor = "غير معروف";\n                if (authorElement != null && !authorElement.text().trim().isEmpty()) {\n                    tempAuthor = authorElement.text().trim();\n                }' app/src/main/java/com/fire/mangareader/activity/MangaDetailActivity.java
sed -i 's/final String finalStatus = tempStatus;/final String finalStatus = tempStatus;\n                final String finalAuthor = tempAuthor;/g' app/src/main/java/com/fire/mangareader/activity/MangaDetailActivity.java
sed -i 's/statusText.setText("الحالة: " + finalStatus);/statusText.setText(finalStatus);\n                        TextView authorText = findViewById(R.id.mangaAuthor);\n                        if (authorText != null) authorText.setText(finalAuthor);/g' app/src/main/java/com/fire/mangareader/activity/MangaDetailActivity.java
sed -i 's/fabFavorite.setImageResource/btnFavorite.setImageResource/g' app/src/main/java/com/fire/mangareader/activity/MangaDetailActivity.java
