sed -i '/import androidx.appcompat.app.AppCompatActivity;/a import com.google.android.material.tabs.TabLayout;' app/src/main/java/com/fire/mangareader/activity/MangaDetailActivity.java
sed -i '/titleText.setText(mangaTitle);/a \
        ImageView btnBack = findViewById(R.id.btnBack);\n        if (btnBack != null) btnBack.setOnClickListener(v -> finish());\n\n        TabLayout tabLayout = findViewById(R.id.tabLayout);\n        View detailsContainer = findViewById(R.id.detailsContainer);\n        View swipeRefreshLayout = findViewById(R.id.swipeRefreshLayout);\n\n        if (tabLayout != null) {\n            tabLayout.addOnTabSelectedListener(new TabLayout.OnTabSelectedListener() {\n                @Override\n                public void onTabSelected(TabLayout.Tab tab) {\n                    if (tab.getPosition() == 0) {\n                        detailsContainer.setVisibility(View.VISIBLE);\n                        swipeRefreshLayout.setVisibility(View.GONE);\n                    } else {\n                        detailsContainer.setVisibility(View.GONE);\n                        swipeRefreshLayout.setVisibility(View.VISIBLE);\n                    }\n                }\n                @Override public void onTabUnselected(TabLayout.Tab tab) {}\n                @Override public void onTabReselected(TabLayout.Tab tab) {}\n            });\n        }' app/src/main/java/com/fire/mangareader/activity/MangaDetailActivity.java
