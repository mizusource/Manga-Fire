<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layoutDirection="rtl">

    <!-- حاوية القارئ الأساسية -->
    <FrameLayout
        android:id="@+id/reader_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- متصفح كلودفلير الخفي (نقطة واحدة لكي يعمل بدون مشاكل) -->
        <WebView
            android:id="@+id/scraperWebView"
            android:layout_width="1dp"
            android:layout_height="1dp"
            android:alpha="0.0" />

        <!-- قائمة الفصول (القارئ) -->
        <com.fire.mangareader.reader.viewer.WebtoonRecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scrollbars="none" />

        <!-- مؤشر الصفحات العائم بالأسفل -->
        <TextView
            android:id="@+id/tvPageIndicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:layout_marginBottom="24dp"
            android:background="#88000000"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:text="1 / 1"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold" />
    </FrameLayout>

    <!-- فلتر حماية العين (الذي برمجناه مسبقاً) -->
    <com.fire.mangareader.reader.viewer.ReaderColorFilterView
        android:id="@+id/colorFilterView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" /> 

    <!-- القائمة العلوية والسفلية (التي تظهر وتختفي عند لمس الشاشة) -->
    <FrameLayout
        android:id="@+id/uiOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

        <!-- الشريط العلوي الاحترافي -->
        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#E6121212"
            app:elevation="0dp">

            <androidx.appcompat.widget.Toolbar
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:contentInsetStart="0dp"
                app:contentInsetEnd="0dp">

                <RelativeLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:paddingHorizontal="8dp">

                    <ImageView
                        android:id="@+id/btnBack"
                        android:layout_width="48dp"
                        android:layout_height="48dp"
                        android:layout_alignParentStart="true"
                        android:layout_centerVertical="true"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:padding="12dp"
                        android:src="@android:drawable/ic_menu_revert"
                        app:tint="#FFFFFF" />

                    <!-- 🚀 تم الإصلاح هنا بإضافة +id لكي لا يحدث خطأ برمجي -->
                    <TextView
                        android:id="@+id/tvChapterTitle"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_centerVertical="true"
                        android:layout_toStartOf="@+id/btnEyeFilter"
                        android:layout_toEndOf="@id/btnBack"
                        android:layout_marginStart="8dp"
                        android:layout_marginEnd="8dp"
                        android:ellipsize="end"
                        android:maxLines="1"
                        android:text="عنوان الفصل"
                        android:textColor="#FFFFFF"
                        android:textSize="18sp"
                        android:textStyle="bold" />

                    <ImageView
                        android:id="@+id/btnEyeFilter"
                        android:layout_width="48dp"
                        android:layout_height="48dp"
                        android:layout_alignParentEnd="true"
                        android:layout_centerVertical="true"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:padding="12dp"
                        android:src="@android:drawable/ic_menu_view"
                        app:tint="#FFFFFF" />

                </RelativeLayout>
            </androidx.appcompat.widget.Toolbar>
        </com.google.android.material.appbar.AppBarLayout>

        <!-- الشريط السفلي (شريط التمرير السريع وأزرار التنقل) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:background="#E6121212"
            android:gravity="center"
            android:minHeight="?attr/actionBarSize"
            android:orientation="horizontal"
            android:padding="8dp">

            <!-- زر الفصل السابق (على اليمين في اللغة العربية) -->
            <ImageButton
                android:id="@+id/btnPreviousChapter"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:padding="12dp"
                android:src="@android:drawable/ic_media_previous"
                app:tint="#FFFFFF" />

            <TextView
                android:id="@+id/tvCurrentPageSeek"
                android:layout_width="36dp"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="1"
                android:textColor="#FFFFFF"
                android:textSize="14sp" />

            <SeekBar
                android:id="@+id/pageSeekBar"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:progressTint="#FF9800"
                android:thumbTint="#FF9800"
                android:max="100" />

            <TextView
                android:id="@+id/tvTotalPagesSeek"
                android:layout_width="36dp"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="20"
                android:textColor="#FFFFFF"
                android:textSize="14sp" />

            <!-- زر الفصل التالي (على اليسار في اللغة العربية) -->
            <ImageButton
                android:id="@+id/btnNextChapter"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:padding="12dp"
                android:src="@android:drawable/ic_media_next"
                app:tint="#FFFFFF" />
        </LinearLayout>
    </FrameLayout>
</FrameLayout>
